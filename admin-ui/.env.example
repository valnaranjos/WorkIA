# =============================================================================
# KOMMO AI ADMIN UI - ENVIRONMENT VARIABLES
# =============================================================================
# Copia este archivo a .env para desarrollo local
# En producción, estas variables se configuran en el servidor

# -----------------------------------------------------------------------------
# API BACKEND
# -----------------------------------------------------------------------------
# URL base del backend (sin /api al final)
# Desarrollo: déjalo en blanco (usa proxy de Vite)
# Producción: URL completa del backend
VITE_API_URL=

# -----------------------------------------------------------------------------
# ADMIN API KEY
# -----------------------------------------------------------------------------
# Debe coincidir con Admin:ApiKey en el backend
VITE_ADMIN_API_KEY=tu-api-key-super-secreta-aqui

# -----------------------------------------------------------------------------
# NOTAS
# -----------------------------------------------------------------------------
# - En desarrollo con `npm run dev`, deja VITE_API_URL vacío
#   El proxy de Vite redirigirá a https://localhost:7000
#
# - En producción (build), configura:
#   VITE_API_URL=https://api.tudominio.com
#   VITE_ADMIN_API_KEY=<tu-key-de-produccion>